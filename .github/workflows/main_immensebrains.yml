name: Build and Deploy Next.js App to Azure Web App - immensebrains

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - name: Install dependencies
        run: npm ci

      - name: Build the app
        run: npm run build

      - name: Create deployment package
        run: |
          mkdir deploy
          cp -r .next public package.json package-lock.json deploy/
          cd deploy && zip -r ../release.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: release
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: release

      - name: Unzip release
        run: unzip release.zip

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_7A48C11708BC4E239C83126682C46B34 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_CA6DEA92CE6946EAB2B28E8C8203CD0F }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_2499B08222764F14A0B330A38F70BD2A }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'immensebrains'
          slot-name: 'Production'
          package: .
